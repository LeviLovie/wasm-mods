package test:guest;

interface foo {
    resource bar {
        constructor(value: s32);

        increment: func();
        value: func() -> s32;
    }
}

interface log {
    log: func(message: string);
}

world example {
    import log;
    export foo;
}
