package module:guest;

interface events {
    resource data {
        constructor();

        init: func();
        update: func(delta: f32);
        shutdown: func();
    }

    info: func() -> list<string>;
}

interface log {
    log: func(message: string);
}

world module {
    import log;
    export events;
}
